мысли:
	реалировать в Group быструю функцию Contains+
	реализовать перегрузку Group.Create чтобы она принимала в качестве аргумента функцию OnAddEntity
	реализовать евент OnGameEnd, который будет вызываться по окончанию рантайма



что сделано:
	Storage<T> переименован в StorageForType<T>
	StorageForType<T> не виден ниоткуда
	удалена функция Add(ComponentBase component)[в EntityBase] за ненадобностью
	добавлен индексатор в группы. теперь к entities можно обращаться someGroup[ent]
	добавлена функция Contains в группы. поиск по массиву entities слудует проводить с помощью нее

	фикс багов
	снижение производительности при удалении и добавлении компонента (сильно)
