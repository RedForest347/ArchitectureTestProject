4)	для ComponentBase сделать CustomStart и CustomAwake в положенные времена (сделать OnFirstEnable и к AddComponent)
5)	удаление синглтонов при остановке сцены (сложное, отложено) -



новое:

-1)	удобное добавление и работа с ISignal -- (необходимо как то инициализировать SignalManager<T> (видимо, только рефлексия))
+2)	Group.OnBeforeRemoveEntity - ошибка при остановке сцены +
+3)	корректное добавление компонентов через код (массив show_comp см. #region Костыль в ENTEditor) +
-4)	запилить систему, которая показывает какие группы существуют впринципе, какие сущности (префабы) включены в них (все сущ. группы и сущности) --(сложно)
-5)	создать список всех сущностей, также запихнуть на SETUP --(сложно)
6)	убрать ошибку при каждом выходе со сцены (Some objects were not cleaned up when closing the scene. (Did you spawn new GameObjects from OnDestroy?))
7)	создать автоматическое создание балванок компонентов/процессингов для их удобного создания через инспектор
+8)	реализовать создание сущности (добавление EntityBase на GameObject) через EntityEditor
-9)	запилить систему управления звуками (требования пока смутные) -- (отсутствие необходимости)
10)	мб реализовать систему доступа к компонентам группы через саму группу
+11)	отключение/вкоючение процессингов из/в GSS +
+12)	при создании нового компонента в списке он не по алфавиту +
+13)	возвращать не NULL, а эксепшон +
+14)	в стартере в настоящий момент возникает проблема, когда группа при Start пытается найти в группе еще 
не инициализированную сущность (Awake сущности произошел после Awake стартера) +
+15) более удобная инициализация евентов группы (OnAddEntity, OnBeforeRemoveEntity) при создании группы +